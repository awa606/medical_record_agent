# Codex读取与任务颗粒度

[返回目录](README.md) · [结构化候选数据](EDGE_CANDIDATE.json) · [15项正式计划快照](PLAN_SNAPSHOT.md)

先阅读README和1.1修订说明，再读取结构化数据。此目录为2026-09-14选型/展示快照，不是新的任务系统或自动同步接口。

## 必须区分的事实

- 当前展示候选是Jetson GPU边缘盒，电脑只采音和操作网页，模型不得移回电脑却声称边缘端通过。
- 旧1.1 DONE属于旧办公主机基线。Jetson未采购、未验收，工程Gate是NEEDS VALIDATION。
- 旧1.2是开发机CPU实验，LLM Mock；ready、healthy或图片不能证明Jetson ARM/CUDA或真实E2E通过。
- 套件参考价2070元，五项缺价。总额和余额是未知，不是2070元/930元的完整方案。
- 正式15任务、87h、依赖、日期、M1–M4与验收标准本次未修改。未来修改以前先读取最新Vault事实；远程Codex无法访问时应说明只能看到此快照。

## 按既有WBS对齐后续验证

| 既有WBS | 需要核对的候选工作 | 最小交付颗粒度 | 当前结论 |
| --- | --- | --- | --- |
| 1.1 | 完整SKU、套件包含关系、六项采购行和外部前提 | 每个部件有选择理由、兼容检查与报价来源；总额可核算 | 仅候选材料完成，实物和完整报价待验证 |
| 1.2 | ARM/JetPack/CUDA/容器兼容，运行盘与模型缓存 | 记录软件版本组合、镜像来源、health与Provider readiness | 未在Jetson执行 |
| 1.3 | 冻结ASR/LLM模型、量化与后端，定义内存释放 | 每个模型有版本/哈希、运行参数和内存记录 | 未冻结Jetson组合 |
| 2.1 | USB麦克风、浏览器权限、局域网HTTPS及上传 | 同一真实样本的设备、采样、请求和结果 | 未在新候选环境验证 |
| 2.2 | 在Jetson执行真实音频转写 | 输入、模型、输出、耗时、RAM和温度 | 未执行 |
| 5.1–5.4 | Smoke、离线、连续5次E2E、缺陷与归档 | 保持原任务交付物和验收证据要求 | 不以本次展示代替 |

以上只是将验证问题定位到原任务，不新建叶子任务、不增加工时、不解除原依赖，也不授权开始开发。若用户随后要求实施，先按项目Engineering Design Review完成必要验证；仍需通过受控更新接口维护状态，不能凭这份文件直接DONE。

## 本次已做的检查

两个工作簿共五个工作表已检查；数量/单价变动、缺价、全部缺价及超预算计算已验证；PNG和Markdown链接已核对。历史源文件哈希、15任务/87h/19技术依赖/6资源依赖/4里程碑均保持原样。这些只证明导出材料可用，不是产品验收。
