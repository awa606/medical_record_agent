# 医生端可交付验收清单

本文用于验收 `v0.6.3` 医生端闭环：输入、转写、角色校正、病历生成、医生审核和导出。

## 验收环境

```powershell
$env:PYTHONPATH = (Get-Location).Path
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

访问：

```text
http://127.0.0.1:8000/static/doctor.html
```

## 验收场景

| 场景 | 操作 | 预期结果 | 结果 | 问题记录 | 截图位置 |
| --- | --- | --- | --- | --- | --- |
| 文本导入生成病历 | 点击“文本导入”，使用默认问诊文本生成病历 | 字段区、候选诊断、安全校验正常显示，下一步提示进入医生审核 | 待测 | 待填 | 待填 |
| 短音频 Mock ASR | 上传短 MP3/WAV，选择 `Mock ASR` | 中间栏出现 SSE 分段、角色标签、校正入口和“用校正文本生成病历”按钮 | 待测 | 待填 | 待填 |
| 中文短音频真实 ASR | 上传中文问诊音频，选择 `SenseVoice` 或 `FunASR` | 转写完成后可进入角色校正；失败时显示可理解错误 | 待测 | 待填 | 待填 |
| 角色校正 | 修改任一分段角色或文本，点击“保存角色校正” | 状态变为已校正，后续病历生成使用校正文本 | 待测 | 待填 | 待填 |
| 16 分钟长音频 | 上传 16 分钟稳定性样本，选择 `SenseVoice` 或 `FunASR` | 中间栏显示切片进度，最终完成或给出明确失败原因 | 待测 | 待填 | 待填 |
| 30 分钟长音频 | 上传 30 分钟稳定性样本，选择 `SenseVoice` 或 `FunASR` | 中间栏显示当前切片、总切片、失败原因和重试提示 | 待测 | 待填 | 待填 |
| 未审核导出阻断 | 生成病历后不确认字段，直接点击导出 | 导出按钮不可用或被后端阻断 | 待测 | 待填 | 待填 |
| 审核后导出 | 点击“保存草稿”“确认字段”“确认导出” | 生成 Markdown/Word 导出文件 | 待测 | 待填 | 待填 |

## v0.6.3 前端展示验收点

- 顶部流程条固定为 6 步：输入、实时转写、角色校正、生成病历、医生审核、导出。
- `下一步` 面板始终显示当前动作，例如上传音频、保存角色校正、生成病历、确认字段、确认导出。
- 转写区显示进度、分段、切片、文件名和角色校正状态。
- 失败时显示失败原因和重试建议，不要求医生打开调试页才能理解问题。
- 导出前必须经过医生确认，不能自动导出。

## 记录规则

- 每次验收后，把实际结果写入本表。
- 发现问题时，在 `logs/debug/` 新建 Debug Log。
- 每次前端改动后，至少执行：

```powershell
node --check static\doctor.js
$env:PYTHONPATH=(Get-Location).Path
pytest -q tests\test_asr_sessions_api.py tests\test_tasks_api.py tests\test_records_api.py
git diff --check
```
