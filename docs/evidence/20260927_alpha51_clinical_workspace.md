# 5.1 临床工作区整体候选：验证记录

**结论：可操作候选已交付，Visual / Release Gate仍NEEDS VALIDATION，5.1保持VERIFY。**

实际时间为2026-09-26夜间至09-27；正式WBS日期不变。代码基线`bf76805dbca378a4644d781693957dde255e1d4a`，PR #113继续Draft。只调整现有医生前端；没有后端API、数据库、Provider、模型或安全阈值改动。

## 同病例对照

两个页面均使用同一匿名合成发热病例及相同fixture，在安装版Edge无头、1440×900测量。旧页面静态文件与基线Git内容核对一致。图片保存在本地`.artifacts/alpha51-clinical-layout-20260926/`，未提交Git。

| 指标 | 旧页面 | 本候选 |
|---|---:|---:|
| 病历起点 | 269.8px | 168px |
| 病历宽度 | 547.5px | 661.5px |
| 病历占三栏内容宽度 | 45.59% | 54% |
| 普通病历组织 | 独立蓝色卡片 | 白色连续章节与细分隔线 |
| 录音操作 | 右抽屉 | 左转写栏内嵌，按状态显示 |
| 窄屏 | 持续争抢三栏宽度 | 先收起参考，再收起转写；明确按钮打开 |

- 同病例普通草稿：`before-draft-1440.png` / `after-draft-1440.png`。
- 同病例长内容：`before-long-1440.png` / `after-long-1440.png`。
- 最终场景：`edge-final/`与`chrome-final/`各30张，覆盖六场景×五视口。每张SHA见同名JSON。
- 不是整齐的空态截图：包含录音、长病历、待审核、角色阻断、Revision冲突；详情打开后未保存字段仍保留。

## 验证结果

| 检查 | 本轮结果与边界 |
|---|---|
| 完整pytest | 555 passed，8 subtests passed，298.35秒 |
| 安装版Edge无头 | 6项通过，含30个场景/尺寸组合；不等于真实125%/150%缩放 |
| 安装版Chrome无头 | 同一候选套件6项通过 |
| 原独立样稿 | 76项通过，仅为旧资产回归，不充当当前页面视觉认可 |
| 前端/fixture语法、Python编译、diff | 通过 |
| health/ready | 隔离8791服务200/200，demo/mock模式；不能证明真实ASR/LLM就绪 |
| 原角色、Revision、审核/导出 | 纳入完整回归；未放宽任何门禁 |
| 实际Edge125%/150% | 未核验，当前控制工具没有Edge通道；不以小视口代替 |
| 新版真实三路径与恢复 | 未执行，按计划在版式认可后验证最终SHA |

## 失败保留与修正

1. 旧测试断言固定抽屉和旧字号，已改成新批准的可见行为；后台取消、角色与导出断言保留。
2. 内嵌录音从工作列表启动时未切到就诊工作区，已修复该入口，并重新通过实际浏览器录音交互回归。
3. 定向回归曾出现一次断网取消后session状态不一致。独立复测和最终完整回归均通过，未修改后端掩盖失败；原日志保留，后续真实三路径继续观察。
4. 首次全量有一项旧抽屉静态断言失败和Windows子进程编码警告；最终使用UTF-8环境重跑通过，无警告。

## 门禁与复现

打开 [本地候选](http://127.0.0.1:8791/workspace-review)，或按[启动与验收说明](../evt/alpha51_clinical_workspace_review.md)重建。该页面直接使用正式doctor DOM，顶部持续标识“合成数据·版式验收”。场景切换会重置合成前端状态，不能用于真实病历。

设计依据见[设计复核](../design_reviews/20260927_alpha51_clinical_workspace.md)。用户版式认可未收到；未更新截图基线，未切2626，未操作2600/2666，不新建冻结包、不启动5.3。旧三路径/r6保留为历史结果。

同轮Project Planner快照：16项99h、19技术/4资源依赖；8 DONE、3 VERIFY、1 BLOCKED、4 BACKLOG。1.1既有Gate违规及四项硬件延期容量提示保持原状。本轮仅追加5.1证据和R04/R11观察，原progress95与验收false不变。视觉返工单独记录工具时间窗口，不冒充已核准人工工时，不静默改变排期。
