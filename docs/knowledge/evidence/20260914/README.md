# 知识库 V1 最小闭环证据（2026-09-14）

## 实际运行结果

| 检查项 | 结果 |
|---|---:|
| 官方来源 | 3 份 |
| 可定位片段 | 30 个 |
| FTS5 Recall@5 | 95%（19/20） |
| BGE 混合 Recall@5 | 95%（19/20） |
| 两种模式引用信息完整率 | 100% |
| 无来源引用 | 0 |
| BGE 向量 | 30 个，512 维 |
| 离线缓存重跑 | PASS |

执行对应 Git SHA：`71f3f46d0650aa6307cb19da97a5c1d5a249db51`。

FTS5 未命中 KQ19，混合检索未命中 KQ15。混合检索在这组开发查询上改变了排序，但没有提高总 Recall@5，因此当前没有开展 BGE 微调，也没有把 T11 标记为 PASS。

## 证据文件

- `source_ingest_and_embedding.json`：来源哈希、页码范围、解析方式、片段数和向量维度。
- `retrieval_fts5.json` / `retrieval_fts5.md`：词法检索逐查询结果。
- `retrieval_hybrid.json` / `retrieval_hybrid.md`：BGE 混合检索逐查询结果。

## 验收边界

这是 20 条开发查询的来源级标签，用于验证知识导入、引用和检索链路。最终 T11 需要至少 120 条复核查询，其中 40 条冻结测试集，并使用片段级相关性标签；在此之前，课程测试状态只能记录为 `PARTIAL`。
