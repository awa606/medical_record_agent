# 2026-07-06 日志

## 今日目标

建立生产级学术工程管理结构，使 Medical Record Agent 具备 Issue、PR、版本、日志和调试记录规范。

## 完成内容

- 新增 `.github/ISSUE_TEMPLATE/` 和 PR 模板。
- 新增 `docs/architecture.md`、`docs/version_log.md`、`docs/debug_guide.md`。
- 新增 `docs/engineering_rules.md`，固化变更控制、目录职责、可追踪性和提交规范。
- 新增 `/logs` 调试和每日日志目录。
- 将 `/versions` 对齐为 `v0.1 basic ASR pipeline`、`v0.2 SSE streaming`、`v0.3 role separation`、`v0.4 medical reasoning`、`v1.0 deployable system`。
- 新增每个里程碑目录的 README，记录目标、验收证据和当前状态。
- 创建 GitHub Issue #1-#5，分别追踪五个标准里程碑。
- 新增 `docs/traceability_matrix.md`，将核心能力映射到 Issue、版本、代码、测试和文档证据。
- 将本地课程手册目录 `report/` 加入 `.gitignore`，避免误提交。
- 完成本地服务烟测，`/health` 返回 `{"status":"ok"}`。
- 更新 README 为项目工程入口文档。

## 验证

- `git diff --check` 通过。
- `$env:PYTHONPATH = (Get-Location).Path; pytest -q` 通过，结果为 `70 passed`。
- `python -m uvicorn app.main:app --host 127.0.0.1 --port 8017` 临时启动后，`/health` 返回 `{"status":"ok"}`。

## 风险

- 本次只修改文档和工程管理结构，不修改核心算法逻辑。
- `report/` 已加入 `.gitignore`，作为本地课程手册目录保留，不纳入版本库。

## 追加记录：v0.2.1 ASR 文件流 SSE

### 今日计划

- 按四周迭代计划先实现“文件流优先”的最小可运行版本。
- 新增 ASR 会话级 SSE 接口，不改现有 ASR 引擎核心逻辑。
- 在医生端中间转写栏实时显示 MP3/WAV 分段转写。
- 补齐四周迭代、每日任务记录、本地模型和边缘端评测文档。

### 今日完成

- 新增 `/api/asr/sessions` 会话接口、音频上传接口、SSE 事件接口和结果读取接口。
- 新增 `tests/test_asr_sessions_api.py`，覆盖会话创建、MP3 上传、SSE 分段事件、断线续传跳过已发送事件和旧 transcript 兼容。
- 更新 `static/doctor.html`、`static/doctor.js`，上传 MP3/WAV 后通过 SSE 将 segment 追加到中间转写栏。
- 新增 `docs/asr_sse_file_stream.md`、`docs/four_week_iteration_plan.md`、`docs/local_model_edge_benchmark.md`、`logs/daily/template.md`。
- 更新 README、架构文档、版本日志、追踪矩阵和 `versions/v0.2_sse_streaming/README.md`。

### 问题与阻碍（Blocker）

- 本机未安装 `gh` CLI，无法直接从命令行创建远端 GitHub Issue。

### 解决方案 / 需求支持

- 已在 `docs/four_week_iteration_plan.md` 中按 GitHub Issue 模板整理 Issue Seed。
- 后续可通过 GitHub 网页或安装 `gh` CLI 后创建远端 Issue，并把 Issue 编号补回追踪矩阵。

### 明日计划

- 验证真实 FunASR 环境下的 MP3/WAV 文件流效果。
- 设计医生/患者角色人工校正入口。
- 扩展知识库症状-疾病-检查-用药关联规则表。

### 验证记录

- `node --check static\doctor.js` 通过。
- `$env:PYTHONPATH = (Get-Location).Path; pytest -q tests\test_asr_sessions_api.py tests\test_audio_api.py` 通过，结果为 `9 passed`。
- `git diff --check` 通过。
- `$env:PYTHONPATH = (Get-Location).Path; pytest -q` 通过，结果为 `73 passed`。
