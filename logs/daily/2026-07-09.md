# 2026-07-09 每日任务记录：Docker 2601 端口与周评审准备

## 今日目标

- 将 Docker 对外访问端口从 `8000` 调整为 `2601`，容器内部仍使用 `8000`。
- 同步 README、Docker 部署文档、版本记录和能力证据矩阵。
- 整理明天周评审展示提纲，说明本周目标、计划、完成情况和演示路线。

## 完成内容

- 更新 `docker-compose.yml`：端口映射改为 `2601:8000`。
- 更新 README 和 `docs/docker_local_deploy.md`：本机访问、局域网访问、健康检查和 Windows 防火墙端口均改为 `2601`。
- 新增 `docs/scoring/v0_6_week_review_script.md`：提供 5 分钟周评审讲法、本周任务清单、展示顺序和风险边界。
- 更新 `docs/版本演进记录.md`：新增 `v0.6.6 Docker 2601 端口与周评审准备`。
- 更新 `docs/能力证据追踪矩阵.md`：Docker 验证命令切换为 `curl http://127.0.0.1:2601/health`。

## 本周主要工作

- 建立中文医患 ASR 主评测，对比 FunASR、SenseVoice、Qwen3-ASR。
- 记录 CER、医学关键词召回、RTF、CPU 和 RSS，用于判断普通 Windows PC 上的模型路线。
- 生成 16/30 分钟长音频稳定性样本并完成压力测试。
- 通过 5 分钟切片规避 SenseVoice 30 分钟整文件失败问题。
- 将长音频切片状态接入真实 ASR SSE，医生端可展示当前切片、总切片、失败原因和重试提示。
- 建立 Docker 本地部署和局域网访问入口。
- 整理医生端可交付闭环、验收清单、演示脚本和截图证据。

## 明天展示重点

1. 项目不是自动诊断系统，而是医患问诊音频到结构化病历的医生审核辅助系统。
2. 本周重点是本地 ASR 评测、长音频稳定性和医生端可演示闭环。
3. SenseVoice/FunASR 当前更适合默认演示路线；Qwen3-ASR 保留为研究候选。
4. 16/30 分钟长音频通过切片机制提高稳定性。
5. Docker 访问地址统一为 `http://127.0.0.1:2601/static/doctor.html`。

## 验证命令

```powershell
docker compose config
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
git diff --check
```

## 实际验证结果

- `docker compose config`：通过，端口发布为宿主机 `2601` 到容器 `8000`。
- `docker compose up -d --build`：通过，容器 `medical-record-agent` 已重建并启动。
- `curl http://127.0.0.1:2601/health`：返回 `{"status":"ok"}`。
- `curl -I http://127.0.0.1:2601/static/doctor.html`：返回 `200 OK`。
- `node --check static\doctor.js`：通过。
- `$env:PYTHONPATH=(Get-Location).Path; pytest -q tests\test_asr_sessions_api.py tests\test_tasks_api.py tests\test_records_api.py`：`15 passed`。
- `$env:PYTHONPATH=(Get-Location).Path; pytest -q`：`110 passed`。
- `git diff --check`：本轮文件无空白错误；当前工作区仍有既有 `data/asr_eval/reports/asr_dependency_check.json` 换行提示，未纳入本次端口变更。

## 前端展示测试

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 先用文本导入生成病历，验证字段区、候选诊断和安全提示。
3. 再用短音频选择 Mock ASR，验证 SSE 分段和角色校正入口。
4. 如 Docker 中模型缓存已就绪，再选择 FunASR 或 SenseVoice 测真实中文音频。
5. 30 分钟长音频现场不强跑完整流程，优先展示已有切片评测和截图证据。

## v0.6.7 真实 ASR 前端验收补齐

- 在 Docker `2601` 医生端补齐真实前端验收。
- 文本导入生成病历通过，生成 10 个字段卡片。
- Mock ASR 上传通过，页面显示 SSE 分段和角色校正入口。
- FunASR 短音频通过，页面显示 `funasr-paraformer-zh · 1段`。
- SenseVoice 短音频通过，页面显示 `sensevoice-small · 1段`。
- SenseVoice 16 分钟长音频通过，切片中显示 `第 1/4 片转写中`，完成后显示 `sensevoice-small-chunked · 4段`。
- FunASR 30 分钟长音频通过，切片中显示 `第 1/6 片转写中`，完成后显示 `funasr-paraformer-zh-chunked · 6段`。
- 新增验收记录：`docs/doctor_workbench_acceptance_v0_6_7.md`。
- 新增截图目录：`docs/final_report/images/v0_6_7_frontend_acceptance/`。
- 新增 Debug Log：`logs/debug/2026-07-09_docker_funasr_first_load_slow.md`。

### v0.6.7 前端展示调整

- 本轮不改 UI 布局，重点是补齐真实 Docker 前端验收。
- 确认医生端能展示真实 ASR 模型名、转写进度、切片编号、总切片数、分段数和角色校正入口。
- 确认长音频完成后页面展示 `*-chunked` 引擎名和合并后的分段数。

### v0.6.7 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
$env:PYTHONPATH=(Get-Location).Path; pytest -q
```

## v0.7.0 医生端视觉产品化

- 参考医疗工作台截图，重构医生端首屏视觉层。
- 将患者、会话、录音、ASR 引擎和审核状态合并到顶部状态栏，减少首屏高度占用。
- 将流程条从按钮式卡片改为圆点进度条。
- 优化左侧病历字段、中间对话转写、右侧 AI 辅助与安全校验三栏比例、卡片边框、间距和状态标签。
- 医生模式隐藏内部调试/截图模式开关，顶部保留首页、上传音频、文本导入等交付操作。
- 新增截图证据目录：`docs/final_report/images/v0_7_0_ui_refresh/`。
- 新增说明文档：`docs/doctor_workbench_ui_refresh_v0_7_0.md`。

### v0.7.0 前端展示调整

- 首屏更接近“顶部状态栏 + 横向流程 + 三栏医疗工作台”的布局。
- 文本导入生成病历后，三栏分别显示病历字段、对话转写和 AI 安全校验。
- 1280 宽度下无横向溢出；2048×1152 宽屏下三栏工作区完整展示。
- 现有上传音频、SSE 转写、角色校正、病历生成、医生审核和导出功能入口保持不变。

### v0.7.0 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
```

前端手工测试：

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 点击“文本导入”，使用默认问诊文本生成病历。
3. 检查顶部状态栏、横向流程条、三栏工作区和底部操作条是否完整显示。
4. 上传短 MP3/WAV，选择 Mock ASR 或真实 ASR，确认转写区仍显示 SSE 分段和角色校正入口。
5. 上传长音频时，确认切片状态仍显示在转写状态面板中。

## v0.7.2 AI 辅助区与操作区重排

- 收束上一轮未提交的 v0.7.1 医生端控件调整：顶部 ASR Engine 改为可选下拉框，右上角改为统一 `输入方式` 菜单，并预留 `录音生成` 提示入口。
- 将医生模式右侧 AI 辅助区改为四块固定卡片：
  - 候选诊断
  - 治疗方案推荐
  - 判断证据
  - 安全校验结果
- 将原页面底部 `当前任务` 操作条移入右侧栏，改名为 `操作区`。
- 保留原按钮 ID 和事件绑定，未修改后端 API、ASRResult、SSE 协议、医生审核和导出接口。
- 新增验收说明：`docs/doctor_workbench_assist_operation_v0_7_2.md`。
- 新增截图证据目录：`docs/final_report/images/v0_7_2_assist_operation/`。

### v0.7.2 前端展示调整

- 右侧不再以医生模式 tab 为主，而是直接展示候选诊断、治疗方案、证据和安全校验。
- 操作按钮从底部横条变为右侧 `操作区`，第一行是重新生成、保存草稿、确认字段，第二行是确认导出。
- 未审核时 `确认导出` 灰色禁用，确认字段后变为可用红色按钮。
- 页面底部不再为任务条预留大块空白。

### v0.7.2 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
$env:PYTHONPATH=(Get-Location).Path
pytest -q tests\test_asr_sessions_api.py tests\test_tasks_api.py tests\test_records_api.py
pytest -q
git diff --check
```

前端手工测试：

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 点击 `输入方式`，选择 `文本生成`。
3. 使用蛇咬伤问诊样例生成病历。
4. 检查右侧四块：候选诊断、治疗方案推荐、判断证据、安全校验结果。
5. 检查 `操作区` 是否在右侧栏内，页面底部不再出现任务条。
6. 保存草稿并确认字段，检查 `确认导出` 从禁用变为可用。
7. 点击确认导出，确认 Markdown / Word 导出流程未受影响。

### v0.7.2 实际验证结果

- `node --check static\doctor.js`：通过。
- `$env:PYTHONPATH=(Get-Location).Path; pytest -q tests\test_asr_sessions_api.py tests\test_tasks_api.py tests\test_records_api.py`：`15 passed`。
- `docker compose up -d --build`：通过，容器 `medical-record-agent` 已重建并启动。
- 浏览器验收：右侧四块卡片均已渲染；操作区在右侧栏内；未审核时导出禁用，确认字段后导出可用；页面无横向溢出。

## v0.7.3 医生端布局遮挡与重叠修复

- 修复右上角 `输入方式` 下拉菜单被顶部栏裁剪的问题。
- 修复右侧 `AI辅助与安全校验` 内容区和 `操作区` 视觉重叠的问题。
- 将右侧栏稳定为三段式布局：标题区、可滚动 AI 内容区、固定操作区。
- 保持 `inputMethodButton`、`regenerateButton`、`saveDraftButton`、`confirmFieldsButton`、`exportButton` 的 ID 和事件绑定不变。
- 新增验收说明：`docs/doctor_workbench_layout_fix_v0_7_3.md`。
- 新增截图证据目录：`docs/final_report/images/v0_7_3_layout_fix/`。

### v0.7.3 前端展示调整

- `输入方式` 展开后完整显示 `录音生成 / 音频生成 / 文本生成`。
- 右侧 AI 内容区单独滚动，不覆盖操作区。
- 操作区固定在右侧栏底部，与 AI 内容区保留 8px 间距。
- 1280 宽度下无横向溢出。

### v0.7.3 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
$env:PYTHONPATH=(Get-Location).Path
pytest -q tests\test_asr_sessions_api.py tests\test_tasks_api.py tests\test_records_api.py
pytest -q
git diff --check
```

前端手工测试：

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 点击 `输入方式`，确认菜单完整显示且没有被裁剪。
3. 点击 `文本生成`，生成一条病历。
4. 检查右侧 AI 四块卡片可以在独立滚动区查看。
5. 检查 `操作区` 不覆盖 AI 内容区。
6. 保存草稿并确认字段，确认导出按钮从禁用变为可用。

### v0.7.3 实际验证结果

- `node --check static\doctor.js`：通过。
- `docker compose up -d --build`：通过，容器 `medical-record-agent` 已重建并启动。
- 浏览器验收：`输入方式` 菜单未被裁剪；右侧 AI 内容区与操作区 `noOverlap=true`；实际间距 `8px`；确认字段后 `确认导出` 可用；页面无横向溢出。

## v0.7.4 标准医生端一屏化布局

- 压缩顶部状态栏、流程条和下一步提示区，减少首屏上方占用。
- 标准医生端改为摘要优先：左侧显示 4 个关键病历字段，中间显示 4 条对话摘要，右侧显示候选诊断、治疗方案推荐、判断证据、安全校验结果 4 个摘要卡。
- 新增统一详情抽屉，完整字段、证据、候选诊断和完整转写通过 `详情` 查看。
- 操作区保持在右侧底部，未修改后端 API、ASRResult、SSE 协议、审核和导出接口。

### v0.7.4 前端展示调整

- 标准版生成病历后，三栏首屏无明显多重滑块。
- 页面无横向溢出，AI 区与操作区不重叠。
- 详情抽屉可展示全部字段、证据和候选诊断。

### v0.7.4 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
```

前端手工测试：

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 点击 `输入方式 -> 文本生成`，生成一条蛇咬伤样例病历。
3. 检查左中右三栏是否分别显示字段摘要、转写摘要、AI 摘要卡。
4. 点击 `查看全部字段、证据和候选诊断`，确认详情抽屉打开。

### v0.7.4 实际验证结果

- `node --check static\doctor.js`：通过。
- `docker compose up -d --build`：通过，容器 `medical-record-agent` 已重建并启动。
- `curl http://127.0.0.1:2601/health`：通过。
- 浏览器验收：标准版生成后无横向溢出；字段、转写、AI 三个主列表均无明显滚动；详情抽屉打开成功，包含 9 个详情区。
- 截图证据：`docs/final_report/images/v0_7_4_one_screen_layout/`。

## v0.7.5 医生端关怀版布局

- 新增 `标准版 / 关怀版` 显示模式切换。
- 关怀版通过 `body.care-mode` 放大字体、按钮、状态标识和行距，适合演示、投屏和长时间使用。
- 关怀版复用标准版接口、按钮 ID 和事件绑定，未修改后端 API、ASRResult、SSE 协议、审核和导出接口。

### v0.7.5 前端展示调整

- 顶部新增关怀版切换控件。
- 关怀版中按钮更大，转写文字更大，右侧操作区按钮更适合点击。
- 关怀版允许必要内容滚动；标准版仍作为一屏化低滚动布局。

### v0.7.5 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
node --check static\doctor.js
```

前端手工测试：

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 点击顶部 `关怀版`。
3. 生成一条文本病历，检查字号、按钮和行距是否明显变大。
4. 检查字段区、转写区、AI 区和操作区不重叠。
5. 点击详情按钮，确认抽屉正常打开。

### v0.7.5 实际验证结果

- `node --check static\doctor.js`：通过。
- `docker compose up -d --build`：通过。
- 浏览器验收：`body` 成功切到 `care-mode`；页面无横向溢出；右侧 AI 区与操作区不重叠；详情抽屉打开成功。
- 截图证据：`docs/final_report/images/v0_7_5_care_layout/`。

## v0.7.6 医生端参考图式布局修复

- 按老师和用户反馈，修复 `v0.7.4/v0.7.5` 过度压缩后导致的“不直观、不美观、信息看不全”问题。
- 标准医生端恢复参考图式三栏医疗工作台：顶部信息栏、流程条、下一步提示、左侧病历字段、中间对话转写、右侧 AI 辅助与安全校验、右侧操作区。
- 左侧不再只显示 4 个摘要字段，恢复展示主诉、现病史、既往史、过敏史、查体、候选诊断、处理建议等完整卡片。
- 中间转写区不再截断每条对话文本，恢复时间、角色、文本逐行展示。
- 右侧 AI 区保留候选诊断、治疗方案推荐、判断证据、安全校验结果四块，底部操作区不覆盖内容。
- 内容超出时只在对应区域内部滚动，不再把整个页面挤成多个紧凑小块。
- 关怀版保留入口，但复用同一布局体系，只放大字号和按钮，避免重新造成拥挤。
- 新增说明文档：`docs/doctor_workbench_reference_layout_v0_7_6.md`。
- 新增截图证据：`docs/final_report/images/v0_7_6_reference_layout/`。
- 新建 GitHub Issue：[#6](https://github.com/awa606/medical_record_agent/issues/6)。

### v0.7.6 前端展示调整

- 空态下三栏都有合理占位，不出现大片无意义空白或拥挤卡片。
- 文本生成后，病历字段、对话转写、AI 辅助和操作区都能在首屏内形成稳定结构。
- 长内容可以在病历字段区、转写区、AI 内容区内部滚动阅读。
- 右侧操作区固定在右栏底部，和 AI 内容保持间距。
- 关怀版切换后字号、按钮和行距变大，但布局不重叠。

### v0.7.6 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
$env:PYTHONPATH=(Get-Location).Path
pytest -q
git diff --check
```

前端手工测试：

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 空态下检查三栏占位是否均衡。
3. 点击 `输入方式 -> 文本生成`，生成一条病历。
4. 检查左侧完整字段卡片、中间完整转写行、右侧 AI 四块和操作区是否一眼可读。
5. 输入长内容时，确认只在对应区域内部滚动。
6. 切换 `关怀版`，确认字号变大且不重叠。
7. 上传 Mock ASR 音频，确认 SSE、角色校正、生成病历、审核导出流程不受影响。

### v0.7.6 实际验证结果

- `node --check static\doctor.js`：通过。
- `docker compose up -d --build`：通过，Docker `2601` 医生端可访问。
- 浏览器验收：页面无横向溢出；空态、生成后、长内容局部滚动、关怀版均有截图证据；右侧 AI 区与操作区不重叠。
- 截图证据：`docs/final_report/images/v0_7_6_reference_layout/`。

### 下一步功能迭代

- `v0.8.0`：真实音频生成流程体验优化。
- `v0.8.1`：角色校正和病历生成联动优化。
- `v0.8.2`：长音频切片状态、失败重试、错误提示优化。
- `v0.8.3`：最终演示路径稳定化。

## v0.7.7 医生端参考图式工作区精准修复

- 根据用户反馈，明确本轮不再修改最上方信息栏，只处理流程条以下的工作区。
- 流程条改为更接近参考图的居中进度线。
- `下一步` 提示区改为轻量横条，只保留当前关键动作和右侧主按钮。
- 三栏工作区重新均衡：左侧病历字段、中间对话转写、右侧 AI 辅助与安全校验。
- 左侧字段卡片恢复更清晰的标题、正文、状态、证据和详情入口。
- 中间转写行恢复表格式阅读，时间、角色和文本更清楚。
- 右侧标准版 AI 区改为四块等分卡片，确保候选诊断、治疗方案推荐、判断证据、安全校验结果四个模块标题都能在首屏看到。
- 操作区固定在右栏底部，与 AI 区保持 12px 间距，不重叠。
- 字段正文不再被 86 字硬截断，改由字段列表内部滚动承载长内容。
- 新增说明文档：`docs/doctor_workbench_reference_workbench_v0_7_7.md`。
- 新增截图证据：`docs/final_report/images/v0_7_7_reference_workbench/`。

### v0.7.7 前端展示调整

- 顶部信息栏保持现状，`topbar` 高度验证为 80px。
- 空态下三栏等高，无横向溢出。
- 生成病历后右侧四个 AI 模块标题均可见。
- 左侧字段列表可局部滚动，页面整体不横向溢出。
- 关怀版无横向溢出，右侧操作区不重叠。

### v0.7.7 怎么测试

```powershell
docker compose up -d --build
curl http://127.0.0.1:2601/health
curl -I http://127.0.0.1:2601/static/doctor.html
node --check static\doctor.js
$env:PYTHONPATH=(Get-Location).Path
pytest -q
git diff --check
```

前端手工测试：

1. 打开 `http://127.0.0.1:2601/static/doctor.html`。
2. 确认最上方信息栏没有变化。
3. 空态下检查三栏占位是否均衡。
4. 点击 `输入方式 -> 文本生成`，生成病历。
5. 确认右侧能同时看到候选诊断、治疗方案推荐、判断证据、安全校验结果和操作区。
6. 滚动左侧字段列表，确认只在字段列表内部滚动。
7. 切换 `关怀版`，确认字号放大但不重叠。
8. 上传 Mock ASR 音频，确认 SSE、角色校正、生成、审核、导出流程不受影响。

### v0.7.7 实际验证结果

- `node --check static\doctor.js`：通过。
- `docker compose up -d --build`：通过。
- 浏览器验收：空态无横向溢出；生成后右侧四个 AI 模块标题均可见；右侧操作区与 AI 内容保持 12px 间距；关怀版无横向溢出。
- 截图证据：`docs/final_report/images/v0_7_7_reference_workbench/`。
