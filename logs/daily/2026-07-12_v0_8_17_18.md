# 每日任务记录：v0.8.17-v0.8.18 说话人分离实测与对齐

## 今日目标

- 提交遗留评审材料并打 tag `v0.8.16`。
- 基于 AliMeeting 公开多说话人样本，补真实 diarization engine 对比。
- 验证 ASR 长 segment 能否用 speaker turn 切分，降低混合语句。

## 完成内容

- 已提交并推送 `v0.8.16`：评审材料、Word/PPT、截图证据、成果表脚本。
- 新增 `scripts/run_diarization_engine_compare.py`，统一比较 FunASR CAM++、pyannote、3D-Speaker。
- 增强 `scripts/evaluate_diarization.py`，支持脚本级复用，并在非空 RTTM 但无 hypothesis turn 时返回 failed。
- 重写 `scripts/check_diarization_dependencies.py`，输出 pyannote、3D-Speaker、FunASR CAM++ 依赖状态。
- 新增 `scripts/apply_diarization_turns_to_asr_result.py`，用于将外部 speaker turns 对齐到 ASRResult。
- 新增对应单元测试。

## 实测结论

- AliMeeting 样本人工 RTTM 实际包含 4 个 speaker、60 个 turn。
- FunASR CAM++ 当前没有输出可对齐的 `hypothesis_turns`，状态为 failed。
- pyannote 当前未安装，状态为 skipped。
- 3D-Speaker 当前未配置独立脚本，状态为 skipped。
- ASR-diarization 对齐脚本可用，但本轮因没有可用 turns，真实结果为 skipped。

## 验证

```powershell
python -m py_compile scripts\run_diarization_engine_compare.py scripts\apply_diarization_turns_to_asr_result.py scripts\evaluate_diarization.py scripts\check_diarization_dependencies.py
$env:PYTHONPATH=(Get-Location).Path
pytest -q tests\test_diarization_evaluator.py tests\test_run_diarization_engine_compare.py tests\test_apply_diarization_turns_to_asr_result.py tests\test_speaker_diarization.py tests\test_run_public_diarization_asr_result.py
```

## 下一步

- 安装 pyannote 或配置 3D-Speaker，取得真实 speaker turns。
- 用 measured turns 重新跑 ASR 对齐，输出 mixed utterance rate 改善数据。
- 前端继续保持 `说话人 A/B/C -> 全局角色映射`，不再逐句猜测医生/患者。
