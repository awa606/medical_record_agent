# 每日任务记录：v0.8.19 说话人分离引擎环境准备

## 今日目标

- 在 v0.8.17/v0.8.18 的真实失败结论基础上，补齐 pyannote 和 3D-Speaker 的环境准备入口。
- 不强制安装大模型依赖，不下载模型权重，不提交令牌或缓存。

## 完成内容

- 新增 `requirements-diarization-experimental.txt`，用于隔离环境安装 pyannote 研究依赖。
- 新增 `scripts/bootstrap_diarization_eval.py`，检查：
  - `pyannote.audio`
  - `pyannote.metrics`
  - `HF_TOKEN`
  - `THREED_SPEAKER_PYTHON`
  - `THREED_SPEAKER_SCRIPT`
- 新增 `tests/test_bootstrap_diarization_eval.py`。
- 生成本机报告：`data/asr_eval/reports/v0_8_19_diarization_bootstrap/diarization_bootstrap_status.md`。
- 修正后续生成的 diarization Markdown 报告中文文案，避免继续产生乱码报告。

## 本机检查结论

- pyannote：blocked，原因是 `pyannote.audio` 未安装且 `HF_TOKEN` 未配置。
- 3D-Speaker：blocked，原因是本机未配置独立 Python 和 wrapper 脚本。
- 当前不能输出 measured diarization 结果，下一步必须先完成至少一个真实分离引擎的环境配置。

## 验证命令

```powershell
python -m py_compile scripts\bootstrap_diarization_eval.py scripts\run_diarization_engine_compare.py scripts\apply_diarization_turns_to_asr_result.py scripts\check_diarization_dependencies.py
$env:PYTHONPATH=(Get-Location).Path
pytest -q tests\test_bootstrap_diarization_eval.py tests\test_run_diarization_engine_compare.py tests\test_apply_diarization_turns_to_asr_result.py
python scripts\bootstrap_diarization_eval.py
```

## 下一步

- 优先配置 pyannote：独立 `.venv-diarization` + `HF_TOKEN`。
- 复测 AliMeeting 样本，输出 measured speaker turns。
- 将 measured turns 接入 ASR 对齐脚本，验证混合语句率是否下降。
