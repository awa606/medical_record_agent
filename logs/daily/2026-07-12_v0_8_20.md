# 每日任务记录：v0.8.20 pyannote 隔离环境安装

## 今日目标

- 把 pyannote 从“包缺失”推进到“环境可导入、只缺 HF_TOKEN”。
- 不提交模型权重、令牌、虚拟环境或原始音频。

## 完成内容

- 将 `.venv-diarization/` 加入 `.gitignore`。
- 固定 pyannote 研究依赖版本：
  - `torch==2.2.2`
  - `torchaudio==2.2.2`
  - `numpy<2`
  - `pyannote.audio==3.4.0`
- 创建 `.venv-diarization`，安装 pyannote 研究依赖和项目基础依赖。
- 修复初次安装出现的 `torchaudio.AudioMetaData` 缺失问题。
- 修复 PyTorch 2.2.2 与 NumPy 2.x 的兼容警告，降级为 NumPy 1.26.4。
- 增强 `scripts/bootstrap_diarization_eval.py`，记录真实 import 状态和版本号。
- 创建 GitHub Issue #19。

## 实测结果

- `pyannote.audio`：可导入，版本 3.4.0。
- `pyannote.metrics`：可导入，版本 3.2.1。
- `torch`：2.2.2+cpu。
- `torchaudio`：2.2.2+cpu，`AudioMetaData=True`。
- `run_diarization_engine_compare.py --engines pyannote`：可运行到 `HF_TOKEN is required for pyannote Community-1`。

## 验证命令

```powershell
.\.venv-diarization\Scripts\python -c "import numpy, torch, torchaudio, pyannote.audio, pyannote.metrics; print(numpy.__version__, torch.__version__, torchaudio.__version__, pyannote.audio.__version__)"
.\.venv-diarization\Scripts\python scripts\bootstrap_diarization_eval.py --reports-dir data\asr_eval\reports\v0_8_20_pyannote_env
.\.venv-diarization\Scripts\python scripts\run_diarization_engine_compare.py --engines pyannote --reports-dir data\asr_eval\reports\v0_8_20_pyannote_env
```

## 下一步

配置本地 `HF_TOKEN` 后进入 `v0.8.21`：真实运行 pyannote Community-1，输出 measured speaker turns，并接入 ASR 对齐脚本复测混合语句率。
