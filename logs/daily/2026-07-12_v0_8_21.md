# 2026-07-12 每日任务记录：Docker 端口恢复与 pyannote 实测门禁

## 当前阶段

`v0.8.21`：Docker 本地入口恢复，继续为 pyannote 真实说话人分离实测做准备。

## 本次完成

- 复现 Docker 启动失败：`2601` 端口无法绑定。
- 定位根因：Windows 当前保留 TCP `2526-2625`，`2601` 位于保留范围。
- 将 Docker 默认宿主机端口切换到 `2626`，容器内部仍为 `8000`。
- 将 `docker-compose.yml` 端口改为 `${MRA_HOST_PORT:-2626}:8000`，支持临时切换端口。
- 新增 `scripts/check_docker_port.py`，用于启动前检查 Docker 宿主机端口是否可绑定。
- 更新 README 和 Docker 本地部署文档。
- 复核 `.venv-diarization`：pyannote 依赖可 import，当前仍缺 `HF_TOKEN`，因此不能进行 pyannote measured 实测。

## 测试记录

```powershell
python scripts\check_docker_port.py --port 2626
docker compose up -d --build
curl http://127.0.0.1:2626/health
curl -I http://127.0.0.1:2626/static/doctor.html
pytest -q tests\test_check_docker_port.py
```

## 前端展示测试

1. 打开 `http://127.0.0.1:2626/static/doctor.html`。
2. 确认医生端页面可以加载。
3. 使用文本生成或 Mock ASR 验证基础流程。
4. 若要真实 pyannote 说话人分离实测，先在本机配置 `HF_TOKEN`，再运行 v0.8.21 后续 measured 命令。

## 问题与处理

- 问题：`2601` 端口不可绑定。
- 处理：改用 `2626`，并保留 `MRA_HOST_PORT` 配置入口。
- 风险：历史验收材料仍写 `2601`，但这些是历史证据；当前运行入口以 README 和 `docs/docker_local_deploy.md` 为准。

## 下一步

- 配置 `HF_TOKEN` 后运行 pyannote Community-1 三说话人实测。
- 将 pyannote diarization turns 与 FunASR ASRResult 对齐，继续降低混合语句率。
- 前端继续优先解决说话人 A/B/C 到医生/患者/其他的一次性全局映射。
