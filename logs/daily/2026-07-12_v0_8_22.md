# 每日任务记录：v0.8.22 说话人分离质量门禁

## 今日目标

继续功能迭代，补齐说话人分离质量门禁，避免 failed/skipped 或质量不达标的 diarization 结果被系统当作可靠医生/患者自动角色映射。

## 本次完成

- 新增 diarization 多引擎对比质量门禁。
- 新增 ASR 与 diarization 对齐结果质量门禁。
- 修复两个报告脚本中的乱码 Markdown 输出。
- 生成 v0.8.22 质量门禁报告。
- 创建 GitHub Issue #21：`feat: v0.8.22 add diarization quality gate`。

## 当前结论

- FunASR CAM++：AliMeeting 三/多人样本没有输出有效 diarization turns，门禁为 `blocked`。
- pyannote：主环境未安装；`.venv-diarization` 中缺少 `HF_TOKEN`，门禁为 `blocked`。
- 3D-Speaker：未配置运行入口，门禁为 `blocked`。
- 当前没有自动分离引擎通过质量门禁，不能直接用于医生/患者自动角色映射。

## 前端展示调整

本轮不改前端 UI。医生端仍通过：

```text
http://127.0.0.1:2626/static/doctor.html
```

后续影响是：只有通过质量门禁的说话人分离结果，才允许进入自动角色映射；否则继续显示说话人或要求全局确认。

## 验证命令

```powershell
python -m py_compile scripts\run_diarization_engine_compare.py scripts\apply_diarization_turns_to_asr_result.py scripts\evaluate_diarization.py
$env:PYTHONPATH=(Get-Location).Path
pytest -q tests\test_run_diarization_engine_compare.py tests\test_apply_diarization_turns_to_asr_result.py tests\test_diarization_evaluator.py
pytest -q
node --check static\doctor.js
curl.exe http://127.0.0.1:2626/health
curl.exe -I http://127.0.0.1:2626/static/doctor.html
git diff --check
```

## 下一步

- 配置 `HF_TOKEN` 后复测 pyannote。
- 配置 3D-Speaker 后复测三/多人样本。
- 只有当某个引擎通过质量门禁，再接入前端自动角色映射候选。
