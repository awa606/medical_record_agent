# 每日任务记录：2026-07-14

## 当前阶段
- 当前版本：`v0.9.6 Release Candidate`。
- 当前定位：不是正式 `v1.0`，本轮完成医生端 UI 最终产品化收束，下一步进入部署端口与核心流程复测。

## 本日完成
- 以专业前端产品审查口径重新检查医生端默认界面，确认当前问题集中在工作区信息层级和 1366 宽度下的拥挤感。
- 在不改后端 API、不改 ASR/SSE、不改病历生成逻辑的前提下，调整医生端工作区样式：左栏病历草稿摘要、中栏对话转写、右栏 AI 辅助与操作区保持三栏结构，但减少默认工程细节。
- 将字段质量、证据、导出阻断和 ASR 细节继续收进详情抽屉，默认界面只保留医生需要先看的摘要。
- 补齐前端截图证据：空态 1366x768、空态 1920x1080、文本生成态、质量详情抽屉。
- 发现 Windows TCP 保留端口覆盖 `2601/2626`，本次 Docker 验收临时使用 `2644`，后续 `v0.9.7` 统一端口预检和文档。

## 修改文件
- `static/doctor.css`：医生端工作区最终产品化样式收束。
- `static/doctor.html`：更新静态资源版本号，确保浏览器加载新样式。
- `docs/final_report/images/v0_9_6_frontend_polish/`：保存本轮 UI 验收截图。
- `docs/版本演进记录.md`：新增 `v0.9.6` 版本记录和当前阶段说明。
- `docs/能力证据追踪矩阵.md`：新增 `v0.9.6` UI 收束证据。

## 验证结果
- `pytest -q`：通过，166 passed，1 warning。
- `node --check static/doctor.js`：通过。
- `git diff --check`：通过。
- Docker 构建与运行：`MRA_HOST_PORT=2644 docker compose up -d --build --force-recreate` 通过。
- Docker 运行态验证：`http://127.0.0.1:2644/health` 返回 `ok`，医生端页面可访问。
- 截图验收：1366x768 和 1920x1080 均已保存证据。

## 未完成与风险
- 当前不是正式交付版，仍需 `v0.9.7` 完成端口策略和核心流程复测，`v0.9.8` 完成周评审材料包。
- 边缘端真实部署暂缓，保留为医院 PC/边缘设备实机条件明确后的 `v1.1` 或后续扩展。
- 工作区仍有未纳入本轮提交的后端抽取器相关改动，本次未暂存，避免混入 UI 收束版本。

## 明日/下一步
- `v0.9.7`：修正 Docker 端口策略，统一 README 与部署文档旧端口说明，复测文本生成、Mock ASR、FunASR 真实音频、角色校正、病历质量、医生确认和导出。
- `v0.9.8`：生成本周评审材料、每日输出、评分表证据、Claude Design PPT 提示词和演示脚本。

## v0.9.7 追加记录：Docker 端口预检与部署体验复测

### 本阶段完成
- 扩展 `scripts/check_docker_port.py`：保留 `--port` 单端口检查，并新增 `--start/--end` 范围扫描和 `--format text|env|json` 输出。
- 更新 `README.md` 和 `docs/docker_local_deploy.md`：不再把 `2601` 或 `2626` 写成固定可用端口，改为启动前扫描 `2600-2699` 并设置 `MRA_HOST_PORT`。
- 创建 GitHub Issue `#28` 跟踪本阶段部署体验复测。

### 验证方式
- `python scripts/check_docker_port.py --start 2600 --end 2699 --format env`：输出可用于 PowerShell 的 `MRA_HOST_PORT=<port>`。
- `pytest -q tests/test_check_docker_port.py`：验证端口解析、排除范围判断和范围扫描推荐逻辑。
- Docker 使用实际可用端口 `2644` 完成健康检查和医生端页面访问。

### 结论
- 当前机器上 `2601/2626` 都可能被 Windows TCP 保留端口范围影响，因此后续演示不再承诺固定端口，统一按脚本推荐的实际端口启动。

## v0.9.8 追加记录：周评审材料包

### 本阶段完成
- 创建 GitHub Issue `#29` 跟踪周评审材料包。
- 新增 `docs/scoring/v0_9_8_week_review_material.md`，整理本周总结、当前真实阶段、每日输出、第一周评分表对应说明、风险和下周计划。
- 新增 `docs/scoring/v0_9_8_claude_design_ppt_prompt.md`，可直接交给 Claude Design 生成 PPT。
- 新增 `docs/scoring/v0_9_8_week_review_talk_track.md`，用于明天现场讲解。
- 生成本地 Word 文件 `homework/Medical_Record_Agent_v0.9.8_week_review_package.docx`。

### 验证结果
- DOCX 结构读取检查通过：33 个段落、5 个表格、1 个 section，文件可由 `python-docx` 正常读取。
- LibreOffice 渲染 QA 两次超时，未生成页面 PNG，因此本轮只能声明“DOCX 已生成并通过结构检查”，不能声明已通过视觉渲染检查。

### 当前阶段口径
- 当前不是正式 `v1.0`，而是 `v0.9.8 Release Candidate`。
- 本周已完成工程闭环与评审材料准备；医院实机、边缘端真实部署和最终验收进入后续阶段。

## v1.0 追加记录：课程工程交付版冻结

### 本阶段完成
- 将当前已提交功能冻结为 `v1.0 课程工程交付版`，不声明临床可用。
- 修复并重写核心 v1.0 文档：`docs/scoring/v1_0_final_freeze_checklist.md`、`docs/doctor_workbench_acceptance_v1_0.md`、`docs/scoring/v1_0_final_demo_runbook.md`。
- 统一最终演示口径：先运行端口预检，使用实际可用端口访问医生端；本机当前推荐端口为 `2644`。
- 明确边缘端真实部署暂缓到 `v1.1` 或后续扩展。

### 验收边界
- v1.0 基于当前已提交 HEAD 冻结。
- 本地仍存在未提交的后端字段抽取实验改动，未纳入本次 Release。
- `homework/` 中的 Word 材料作为本地课程材料保留，不随 GitHub 默认提交。

### 下一步
- 若继续迭代，进入 `v1.1`：医院 PC 实机复测、边缘端部署验证、ASR/角色识别真实样本扩展。

## v1.1 追加记录：医生端信息降噪与后续产品化路线

### 本阶段完成
- 创建 GitHub Issue `#30`，跟踪 `v1.1` 医生端信息降噪。
- 按老师反馈继续收敛医生默认界面：隐藏顶部 ASR Engine 工程选择项，医生不需要在默认界面理解模型细节。
- 右侧 AI 辅助默认只保留三块：候选诊断、治疗方案、诊断证据；病历质量摘要、证据全文、ASR 细节和导出阻断详情继续放在详情抽屉或显示设置中。
- 压缩对话转写空态和识别中说明，减少默认界面解释性文字。
- 新增 `docs/v1_1_to_v1_4_product_roadmap.md`，明确后续 `v1.2` 字段抽取增强、`v1.3` ASR/角色质量优化、`v1.4` 医院 PC 与边缘端评估。

### 验收重点
- 默认医生端只展示关键病历字段、转写摘要、候选诊断、治疗方案、诊断证据和确认导出。
- 模型、质量、证据全文等低频信息不丢失，但不再抢占主界面。
- 本轮不修改后端 API、不修改 ASR/SSE、不修改审核和导出流程。

### 后续计划
- `v1.2`：整理本地字段抽取实验，正式增强主诉、现病史、既往史、过敏史、查体、初步诊断和处理建议。
- `v1.3`：继续优化 ASR 角色质量，减少碎片化和明显错分。
- `v1.4`：拿到医院真实电脑配置后做实机复测；边缘端部署暂不作为当前评审阻塞项。

## v1.2 追加记录：接口化收敛与字段抽取实验清理

### 本阶段完成
- 清理未提交的 `medical_field_extractor/` 实验目录和对应实验测试，避免正式程序继续引用临时包。
- 新增系统能力发现接口 `GET /api/capabilities`，用于说明当前系统可复用能力、接口路径、模型依赖和领域迁移边界。
- 新增无副作用记录服务接口：`POST /api/records/extract-fields`、`POST /api/records/build-draft`、`POST /api/records/quality`。
- 新增 `docs/api_capability_contract.md`，记录接口契约、迁移说明和安全边界。
- 更新版本演进记录、能力证据追踪矩阵和 v1.1-v1.4 路线图，将 v1.2 实际目标修订为“接口化收敛优先”。

### 验收目标
- 新接口不创建 Task、不写数据库、不允许直接导出。
- 医生端原有流程、ASR、审核和导出接口保持兼容。
- Docker 重建后 `/api/capabilities` 可访问。
- `pytest -q`、`node --check static/doctor.js`、`git diff --check` 通过。

### 下一步
- `v1.3`：补充接口迁移示例、API 请求/响应样例、外部调用说明和接口契约测试。

## v1.3.0 追加记录：医生端可用性、查体抽取与截图门禁

### 今日目标
- 修复真实演示截图中暴露的流程条遮挡、右侧 AI 辅助拥挤、详情抽屉遮挡和查体字段误抽取问题。
- 建立后续前端/流程迭代的截图验收门禁。

### 已完成
- 创建 GitHub Issue `#32` 跟踪 v1.3.0。
- 查体字段抽取收紧为客观体征/生命体征规则，避免把医生追问、患者既往史或家族史写入查体。
- 医生端处理建议占位文案改为明确的“需医生结合问诊、查体和检查结果确认”。
- 流程条、详情抽屉、右侧 AI 辅助区和操作区追加 v1.3.0 布局修复样式。
- 新增 `docs/frontend_screenshot_acceptance_gate_v1_3.md`，规定后续每次前端/流程迭代必须截图并人工自查。

### 待验证
- 运行后端、前端、Docker 回归测试。
- 使用浏览器 100% 缩放采集 1366x768 和 1920x1080 截图，检查无遮挡、无重叠、无信息过载。

### 验证结果
- `pytest -q tests/test_prompts_and_mock_llm.py tests/test_mock_llm_fever.py tests/test_records_api.py`：15 passed。
- `pytest -q`：168 passed, 1 warning。
- `node --check static/doctor.js`：通过。
- `git diff --check`：通过，仅提示中文 Markdown 文件换行符将由 CRLF 转为 LF。
- Docker 镜像构建成功；旧容器重建时遇到 Docker zombie 进程无法停止的问题，已改用本地 2650 服务完成最新代码截图验收。此前 2644 容器 `/health`、医生端页面和 `/api/capabilities` 均返回 200。
- 截图证据已保存到 `docs/final_report/images/v1_3_0_usability_quality/`。

### 截图自查结论
- 1366x768 空态：流程条无遮挡，三栏和操作区可见。
- 1366x768 输入方式：下拉菜单完整显示，不被流程条裁剪。
- 1366x768 生成态：流程条无遮挡，AI 辅助区不再挤压操作区；查体字段保持“待医生查体补充”。
- 1366x768 详情抽屉：抽屉默认右侧显示，未全屏覆盖工作区。
- 1920x1080 生成态：候选诊断、治疗方案、诊断证据和操作区均可见，整体布局稳定。

## v1.3.1 追加记录：ASR 转写角色展示稳定化

### 今日目标
- 修复医生端转写区中低置信度自动角色直接显示为“医生/患者”的问题。
- 保留声学说话人和临床角色两层概念：未人工确认或置信度不足时先显示“说话人 A/B/C”，全局映射后再统一替换为医生/患者/其他。
- 不修改 ASR、SSE、病历生成、审核和导出接口。

### 已完成
- 创建 GitHub Issue `#33` 跟踪 v1.3.1。
- `static/doctor.js` 新增角色展示置信度阈值 `0.9`。
- 低于阈值且未人工确认的自动角色不再显示为医生/患者，前端改为显示“说话人 A/B”。
- 全局角色确认门禁同步使用同一阈值，避免低置信度角色直接进入实时预览或正式病历。
- 更新 `static/doctor.html` 中 JS 静态资源版本号。
- 新增 `docs/doctor_workbench_asr_display_v1_3_1.md` 和截图证据目录 `docs/final_report/images/v1_3_1_asr_display/`。

### 验证结果
- `node --check static/doctor.js`：通过。
- `pytest -q tests/test_speaker_diarization.py tests/test_asr_sessions_api.py tests/test_records_api.py`：27 passed, 1 warning。
- `pytest -q`：168 passed, 1 warning。
- Docker：`MRA_HOST_PORT=2600 docker compose up -d --build --force-recreate` 通过，`/health` 与 `/static/doctor.html` 均返回 200。
- 截图自查：
  - `01_low_confidence_speaker_alias.png`：低置信度自动角色显示为“说话人 A/B”，强置信度或人工确认段显示医生/患者，通过。
  - `02_transcript_detail_role_mapping.png`：详情抽屉可按说话人统一校正，通过。
  - `03_after_global_role_mapping.png`：全局映射后同一 speaker 的发言同步显示医生/患者，通过。

### 边界说明
- 本轮没有提升 ASR 文本准确率，也没有新增麦克风实时录音。
- 真实多说话人音频的 speaker 聚类仍可能出错；本轮目标是避免低置信度结果被误展示为确定医生/患者。
