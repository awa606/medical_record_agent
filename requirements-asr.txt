funasr>=1.3.12,<2
modelscope>=1.10.0
# PyTorch is required by FunASR. CPU install:
# python -m pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch
torchaudio
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.1
