# Optional research dependencies for true speaker diarization evaluation.
# Install in an isolated environment, not in the main runtime by default:
#   py -3.11 -m venv .venv-diarization
#   .\.venv-diarization\Scripts\python -m pip install -r requirements-diarization-experimental.txt
pyannote.audio>=3.1,<4.0
pyannote.metrics>=3.2,<4.0
