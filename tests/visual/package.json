{
  "name": "mra-doctor-workspace-visual-tests",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "playwright test",
    "test:visual": "playwright test --config=playwright.visual.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
